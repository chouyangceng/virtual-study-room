# 虚拟自习室 1.2.0 升级审查记录

审查日期：2026-08-14

## 第一轮：逻辑与数据统一

- 合并每日任务和番茄钟任务，消除重复输入和状态不同步。
- 新增今日任务/每日坚持两种任务模型。
- 每日坚持保存完成日期列表，以早上 8 点为边界刷新，并动态计算连续天数。
- 任务记录累计番茄数、专注分钟和最后专注时间。
- 旧每日计划迁移到统一任务，避免升级后丢失。

## 第二轮：界面、流程和模块清理

- 多目标在首页并列展示，管理窗口支持连续新增、编辑、删除。
- 计划中心仅保留周计划、月计划；日级内容转入每日任务。
- 删除白噪音、背景音乐、学科中心及其脚本、入口、模板、同步和导出字段。
- 删除 48 格日计划和阅读计划入口，消除重复规划路径。
- 完成整表删除联动，同来源任务和计划会一起移除。
- 计时结束增加系统通知，不使用振动。
- 使用 Lucide 本地 SVG 替换主要按钮图标，保持离线可用。
- 修复全景模式圆环、数字、任务选择和控制按钮的层级/尺寸冲突。

## 验证

- `npm run check:js`：通过。
- `npm test`：12/12 通过。
- 浏览器交互：任务同步、坚持打卡、多目标、计划精简通过。
- 全景视觉检查：数字完整位于圆环内。

## 发布范围

本轮只发布 Windows 1.2.0。Mac 版按要求暂不修改和构建。

## 第三轮：Windows 轻量化与打不开修复

- 定位打不开原因为测试残留的 Node 归档服务占用 43110/43112 端口，已停止残留进程。
- 新增 `tools/LightweightLauncher.cs`，使用系统 Edge 的应用模式承载界面，并按需隐藏启动 Node 本地归档服务。
- 启动器不再捆绑 Electron/Chromium；启动器为 7,680 字节，业务程序共 1,456,286 字节（1.39 MB）。
- 轻量版部署到 `E:\虚拟自习室\轻量版`，桌面快捷方式已切换到该版本。
- 旧 Electron Local Storage 已复制到轻量版独立 Edge 配置，原 `E:\虚拟自习室\学习数据` 完整保留。
- Edge 缓存重定向到系统临时目录；运行时浏览器配置属于用户数据，不计入发布程序体积。
- 已删除桌面 67.82 MB 便携包、E 盘 Electron 安装目录、源码构建目录及本地旧 Windows 1.1/1.2 安装包；Mac 包保留。

## 轻量版复验

- 启动后主窗口标题为“虚拟自习室 - Virtual Study Room”。
- `GET /index.html` 返回 200，本地归档健康接口返回 `ok: true`。
- `npm run check:js`：24 个 JavaScript 文件全部通过。
- `npm test`：12/12 通过。

## 第四轮：顶部简化、模板与复盘升级

- 顶部由 13 个并列按钮简化为“复盘、全景、更多”3 个入口。
- 更多菜单按“学习管理 / 数据工具 / 显示与计时”分组，保持所有原功能可达。
- “导入计划”和“下载模板”合并到“导入与模板”窗口，形成下载—填写—导入的单一路径。
- 统一模板更新至 2026.08，新增填写说明、当前任务模型示例、分类路径与标签示例，并加宽 Excel 列宽。
- 复盘逻辑改为：完成任务自动进入待复盘 → 填写结果事实、产出证据、下次第一步 → 今日收尾形成结论与明日动作。
- 完成任务不再强制弹出复盘窗口；全部完成后仅提醒用户进入复盘中心。
- 新增“过往复盘”页签，按日期汇总完成数、复盘数、专注分钟、整日结论、明日动作、AI 点评和逐项复盘，并支持全文搜索。
- 新图标采用“打开的书 + 专注光点”，已生成 SVG、PNG、ICO 并应用到桌面快捷方式、轻量启动器和 PWA。
- 实机浏览器验证：更多菜单、导入与模板、今日/过往复盘切换正常；375px 宽度无横向溢出；控制台无错误。

## 第五轮：顶部图标重排

- 依据图标设计规范统一为 44px 点击区域、20px 图标、1.8px 圆角描边，复盘/全景/更多保持同一视觉层级。
- “复盘”取消常驻中文和大面积蓝色按钮，改用勾选笔记图标与轻量强调底色。
- 功能名称仅在鼠标悬停或键盘聚焦时展示，保留完整 `aria-label`，兼顾简洁与可访问性。
- 左侧品牌图形由线框笔记本替换为新版“书本 + 专注光点”应用图标。
- 桌面 1280px 实机截图检查通过；默认顶部仅显示品牌名和 3 个图标，悬停提示与更多菜单均正常，控制台无错误。

## 第六轮：番茄钟—任务—日终复盘完整闭环

- 番茄钟新增“本次备注”，每次专注记录继续绑定同一个每日任务，因此一项任务可对应多个独立番茄钟。
- 每次专注结束自动弹出可跳过的“本次专注复盘”，记录备注、完成情况、状态和下次接续动作；保存或跳过后才自动开始休息。
- 左侧任务勾选完成时自动弹出可跳过的“任务完成复盘”；最后一项任务处理完复盘后，自动进入复盘中心。
- 复盘中心按任务聚合番茄次数、专注分钟、开始前备注、单次复盘和任务完成复盘；未填写的可选复盘字段不生成空白展示。
- 今日收尾保存“一日复盘”和逐行“明日提醒”。明日提醒会生成下一学习日的普通任务，早上 8 点刷新后在左侧出现，仍可正常完成或删除。
- 新增 7 列迷你复盘日历：过去/今日日期查阅当日完整记录；未来日期可新增和删除待办，待办到期后进入左侧每日任务。
- `sessionReviews` 作为独立数据集进入手工备份、Windows 归档、恢复与安全清理流程；旧 `dailyReviews`、`dailyCloseEntries` 和 `focusSessions` 保持兼容。
- Service Worker 缓存升级为 `virtual-study-room-v8`。

## 第六轮验证

- `npm run check:js`：24 个 JavaScript 文件通过。
- `npm test`：12/12 通过。
- 真实 1 分钟计时：结束后弹出单次复盘，保存后自动开始休息，复盘中心正确显示 1 个番茄、1 分钟、备注、结果、状态和接续动作。
- 真实任务闭环：完成任务弹出任务复盘；最后一项保存后自动进入复盘中心。
- 明日提醒与未来待办：两条日终提醒同步到次日，日历继续新增第三条待办成功。
- 375px 手机视口：文档宽度与视口均为 375px，无横向溢出；浏览器控制台无应用错误。
- 已部署到 `E:\虚拟自习室\轻量版`；29 个业务文件共 1.72MB，启动器 80,896 字节，桌面快捷方式继续指向该启动器。

## 第七轮：复盘触发节奏校正

- 保持番茄钟结束自动弹出“单次复盘”，保存或跳过后再进入休息。
- 保持左侧任务勾选完成时自动弹出“任务复盘”。
- 取消全部任务完成后自动打开复盘中心；现在仅做一次轻提示，每日总结由用户主动进入复盘中心完成。
- 复盘中心今日页继续支持修改单次复盘和任务复盘；历史日历详情新增“修改单次复盘 / 修改任务复盘”入口。
- 历史编辑会合并并更新日终快照，避免旧快照覆盖修改后的内容；编辑弹窗层级高于复盘中心，保存后回到原历史日期；缓存升级为 `virtual-study-room-v10`。
