# 虚拟自习室 1.2.1 严格审计报告

审计日期：2026-08-16

## 审计结论

本轮按发布阻断标准检查计时、任务、复盘、归档同步、静态服务、轻量启动器、Electron 容器、依赖安全与键盘可访问性。发现的问题均按数据完整性或安全边界优先修复。

## 已修复问题

1. 删除单条专注记录时，同步清理关联单次复盘、日终番茄快照，并重算当日分钟、任务番茄数、任务专注分钟和最后专注时间。
2. 清空专注记录时，不再让日终快照和孤立复盘“复活”已删除的记录。
3. 手工 JSON 备份版本改为跟随归档核心协议，消除客户端版本 2、服务端版本 3 的漂移。
4. 修复导入数据中的目标 ID 与专注日期进入 HTML 属性/文本时未转义的问题。
5. 本机 `/api/v1/local-config` 不再向 `Origin: null` 或其他跨源页面返回 CORS 许可，避免本地恶意 HTML 读取同步 token。
6. 本地静态页面增加 CSP、禁止嵌入、关闭 referrer，并继续限制可访问的业务文件范围。
7. Windows 轻量启动器不再把任意 HTTP 200 服务误认为自习室，必须同时核对服务名和 API 版本。
8. Electron 限制主窗口导航和新窗口外链协议，只允许可信本地入口；权限仅对可信入口开放通知与全屏。
9. 11 个弹窗补齐 dialog 语义、标题关联、Esc 关闭、Tab 焦点循环和关闭后的焦点恢复。
10. 计划页签补齐 `aria-selected` 状态；已保存但任务被删除的任务复盘仍可在今日复盘中显示和编辑。
11. Electron 从 31 升级至 43.4，electron-builder 从 24 升级至 26.15.3；npm 安全审计由 1 critical、10 high 降为 0。

## 验证门槛

- JavaScript 静态语法检查：通过。
- Node 自动化测试：14/14 通过。
- 单文件构建：通过。
- Windows 归档 HTTP smoke：通过。
- npm audit：0 vulnerabilities。
- .NET Framework 轻量启动器编译：通过。
- 桌面浏览器：dialog 语义、Esc、焦点恢复、复盘中心及控制台检查通过。
- Service Worker 的 21 个离线资源均存在，缓存版本已更新为 `virtual-study-room-v15`。
- Windows 轻量版已部署至 `E:\虚拟自习室\轻量版` 并通过 43110 健康检查；安装后的主页面、离线缓存和单文件版与源码哈希一致。
- `E:\虚拟自习室\学习数据` 未参与程序覆盖，部署前后均为 111 个文件、13,111,472 字节。
- electron-builder 26.15.3 命令行可运行，但本机 Electron 43.4 运行时二进制因镜像下载不可用而未落盘，因此本轮没有把 Electron 桌面包的本机启动列为“已通过”；Windows 正式使用并验证的是轻量版。

## macOS 发布限制

代码与打包配置支持 Intel/Apple Silicon，但从 Windows 不能完成 Apple 公证。未使用 Apple Developer ID 签名和 notarization 的 DMG 仍可能被 Gatekeeper 拒绝；这不是通过修改网页代码就能可靠绕过的问题。正式无警告安装需要在 macOS 构建机配置 Apple 证书、公证凭据后重新出包。
