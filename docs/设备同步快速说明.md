# 设备同步快速说明（1.2.0）

Windows 版继续作为权威归档端。其他设备上传快照，只有 Windows 已持久化并返回有效回执后，客户端才允许清理已确认归档的旧历史。

新版归档字段包括：

- 番茄记录和每日统计
- 统一任务与每日坚持历史
- 周计划、月计划
- 课表
- 多目标倒计时
- 每日复盘、收尾记录
- 计时器和应用设置

白噪音/音频设置、学科目标和当前学科不再同步。旧归档可以继续读取，未被新版采用的字段会被忽略。

手工 JSON 导入会覆盖同名本地字段，导入前请先导出当前数据。同步 token 和 API Key 不会写入导出文件。
