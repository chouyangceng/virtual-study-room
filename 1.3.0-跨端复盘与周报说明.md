# 虚拟自习室 1.3.0 跨端复盘与周报说明

更新日期：2026-08-16

## 同步结论

Mac、平板和手机的计时与复盘数据会作为完整快照上传到 Windows。Windows 每次先原子落盘、复读校验哈希，再返回持久化回执；客户端只有核对回执成功后才允许按保留天数清理旧历史。

本版修复了 Mac Electron 页面与 Windows 服务不同源时的 CORS 预检阻断。业务 API 允许来自 HTTP/HTTPS 应用页面的跨源请求，但所有快照、列表、下载和汇总接口仍强制校验 bearer token。本机配置接口不开放 CORS，且只允许 Windows 回环地址读取。

Windows 汇总接口读取全部不可变快照，并按记录 ID 合并同一记录的较新版本。这样即使其他设备上传成功后清理了本地旧历史，Windows 仍能保留和查阅早先已经归档的计时与复盘。

## 复盘中心

复盘中心改为接近全屏的学习档案界面：

- 今日整合：按任务汇总计时、单次复盘和任务完成复盘，没有任务 ID 的独立计时也不会遗漏。
- 每日档案：按日历阅读历史计时、任务复盘、每日总结和 AI 点评。
- 每周报告：汇总专注分钟、计时次数、学习天数、完成任务、复盘证据和下一步行动。

来自 Windows 归档的其他设备记录带设备来源且为只读，避免在查看汇总时误删原设备记录。

## DeepSeek

支持官方 `https://api.deepseek.com/chat/completions`，模型可选 `deepseek-chat` 或 `deepseek-reasoner`，也允许填写 HTTPS 的 OpenAI 兼容代理地址。本机 HTTP 代理仅允许 localhost/127.0.0.1/::1。

API Key 只保存在当前设备的本地存储。归档核心会递归删除 API Key、token、Authorization、password、secret 和 credential 等敏感字段；生成后的周报正文可以正常同步到 Windows。

## 验证

- JavaScript 静态检查：24 个文件通过。
- Node 自动化测试：16/16 通过。
- 跨源预检、token 鉴权、双设备上传、全部快照汇总和敏感字段隔离均有自动化测试。
- 浏览器实测：完整复盘对话框、三个视图、周报生成、保存、自动上传和 Windows 汇总通过。
- 中等宽度视口复验：复盘内容区 `scrollWidth === clientWidth`，无横向溢出。

## 使用边界

设备之间必须网络可达。局域网环境请确保 Windows 防火墙允许 TCP 43110，且路由器未启用访客网络隔离。公网同步不应直接暴露 43110，建议通过可信 VPN 或 HTTPS 反向代理。

macOS 安装包是否被 Gatekeeper 接受与数据同步是两件事。正式无警告安装仍需要 Apple Developer ID 签名和 notarization。
