# 虚拟自习室 1.4.0 三端功能更新

更新时间：2026-08-20

## 使用体验

- 每周报告使用复盘中心统一纵向滚动，不再把长正文困在狭窄的嵌套滚动区；正文顶部和底部都有快速跳转按钮。
- 番茄钟在开始后显示“提前结束”，专注满 30 秒可按实际分钟保存，记录包含 `endedEarly` 标记并正常进入单次复盘。
- 多目标保存后立即回到“添加新目标”，可以连续添加多个未来日期或坚持天数目标。
- 新建任务分类为数学、英语、专业课、政治、课程学习、其他（自定义）。旧任务的原分类保持不变。
- 专注统计图表在弹窗可见后再初始化，失败时保留文字数据并提供重试按钮。
- 成就由 17 个扩充为 29 个，新增“摸鱼页面不存在”“椅子焊住了”“地球另一边的早起”等。

## 三端与归档

- Windows 继续作为权威数据中心，保存不可变快照和 SHA-256 持久化回执。
- Mac 与 Android 只需配置 Windows 地址和 token，即可同步计时、任务、目标、每日/单次复盘和周报。
- Android 工程位于 `android/`，构建时直接复制当前桌面前端资源，避免功能分叉。
- DeepSeek API Key、同步 token 和其他敏感凭据不会进入 Windows 快照。
